# Dateien für user86000/harvia_fenix

Kopiere diese Dateien in dein Repository:

- `.github/workflows/validate.yml`
- `hacs.json`

Danach committen und pushen:

```bash
git add .github/workflows/validate.yml hacs.json
git commit -m "Add HACS and hassfest validation"
git push
```
